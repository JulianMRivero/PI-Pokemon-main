import React from "react"
import style from "./ColorTypes.module.css"

export const colors = ({types}) => {
    if(types[0] === 'fire') return style.fire
    if(types[0] === 'grass') return style.grass
    if(types[0] === 'steel') return style.steel
    if(types[0] === 'water') return style.water
    if(types[0] === 'dragon') return style.dragon
    if(types[0] === 'electric') return style.electric
    if(types[0] === 'fairy') return style.fairy
    if(types[0] === 'ice') return style.ice
    if(types[0] === 'fight') return style.fight
    if(types[0] === 'normal') return style.normal
    if(types[0] === 'psyco') return style.psyco
    if(types[0] === 'rock') return style.rock
    if(types[0] === 'sinister') return style.sinister
    if(types[0] === 'ground') return style.ground
    if(types[0] === 'poison') return style.poison
    if(types[0] === 'flying') return style.flying
  }
 export const colors1 = ({types1}) => {
    if(types1[1] === 'fire') return style.fire
    if(types1[1] === 'grass') return style.grass
    if(types1[1] === 'steel') return style.steel
    if(types1[1] === 'water') return style.water
    if(types1[1] === 'dragon') return style.dragon
    if(types1[1] === 'electric') return style.electric
    if(types1[1] === 'fairy') return style.fairy
    if(types1[1] === 'ice') return style.ice
    if(types1[1] === 'fight') return style.fight
    if(types1[1] === 'normal') return style.normal
    if(types1[1] === 'psyco') return style.psyco
    if(types1[1] === 'rock') return style.rock
    if(types1[1] === 'sinister') return style.sinister
    if(types1[1] === 'ground') return style.ground
    if(types1[1] === 'poison') return style.poison
    if(types1[1] === 'flying') return style.flying
  }